apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: email-service
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: email-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s