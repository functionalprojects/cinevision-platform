apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: eureka-server
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: eureka-server
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s