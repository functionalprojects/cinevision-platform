apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: movie-service
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: movie-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s