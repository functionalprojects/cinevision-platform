apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: cinevision
spec:
  selector:
    app: user-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP